## v0.3.13

本版本以稳定性为第一优先级：恢复 v0.3.10 已验证的界面与窗口托管视觉基线，在不改变现有顶层窗口托管模型的前提下，增强 WhatsApp 自动接入和账号级消息提醒。

### 稳定视觉基线

- 主界面恢复到 v0.3.10 的成熟容器视觉与布局。
- 撤回 v0.3.11 / v0.3.12 的页面化视觉实验和托管期 DWM 去圆角/去边框覆盖。
- 保留客户端完整原生标题栏、边框、圆角和阴影。
- 保留现有顶层 HWND 托管方式，不使用 `SetParent + WS_CHILD`，不裁客户端标题栏。

### WhatsApp 自动接入

- 启动聚窗时，对“已经发现但第一次接入失败”的窗口进行两次短暂自动重试。
- 后台 5 秒扫描会继续重试自动接入失败的窗口。
- 用户主动“弹出窗口”后会进入保护状态，不会被后台扫描重新抓回。
- 用户明确重新嵌入或执行“全部接入”时解除保护。
- WhatsApp Store/WebView 版本主窗口标题尚未填充时，只要进程和窗口条件有效，也允许进入候选，降低启动时漏检概率。

### 消息提醒

- 不读取聊天正文或消息数据库。
- 客户端触发 Windows 注意请求时，聚窗会定位到具体账号并显示红点。
- 高置信度角标识别继续显示具体未读数量；无法可靠读取数字时显示红点。
- 聚窗任务栏图标会持续闪烁，效果接近微信原生任务栏提醒。
- 聚窗最小化到托盘后收到消息，只重新暴露最小化任务栏按钮并闪烁，不恢复窗口、不抢前台。
- 低频角标识别继续作为 Shell 通知漏报时的兜底；首次启动时已有历史未读不会被当作新消息重复提醒。

### 质量控制

- 新增 Windows PR Build Check。
- 主程序与 BadgeProbe 在合并前自动执行 restore + Release build。
- 本版本对应 PR 已通过 Windows 编译检查。

### 下载说明

- `JuChuang-v0.3.13-FrameworkDependent-win-x64.zip`
  - 轻量版
  - 需要已安装 .NET 8 Desktop Runtime x64
- `JuChuang-v0.3.13-SelfContained-win-x64.zip`
  - 免运行库版
  - 无需额外安装 .NET 运行库

支持 Windows 11 x64。当前发布包未进行商业代码签名，Windows SmartScreen 首次运行时可能显示提示。