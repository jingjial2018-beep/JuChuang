## v0.3.11

本版本重点调整聚窗主界面的视觉层级，让微信与 WhatsApp 更像聚窗自身页面，而不是嵌在容器中的悬浮窗口。

### 界面改进

- 顶部工具栏改为更轻量的文字按钮，降低按钮边框与卡片感。
- 左侧账号栏默认宽度由 292px 缩至 238px，账号项更紧凑。
- 账号选中态改为低对比灰色圆角背景，不再显示强调色竖条。
- 折叠右侧单独的当前账号状态栏，客户端内容直接贴合顶部工具栏。
- 中间分隔区减少视觉占位，让客户端与导航衔接更自然。
- 工作区统一为浅灰/白色体系，降低“窗口套窗口”的层级感。
- 保留现有顶层窗口托管方案，不改为 `SetParent + WS_CHILD`，避免输入、弹窗、DPI 与多开回归。

### 下载说明

- `JuChuang-v0.3.11-FrameworkDependent-win-x64.zip`
  - 轻量版
  - 需要已安装 .NET 8 Desktop Runtime x64
- `JuChuang-v0.3.11-SelfContained-win-x64.zip`
  - 免运行库版
  - 无需额外安装 .NET 运行库

支持 Windows 11 x64。当前发布包未进行商业代码签名，Windows SmartScreen 首次运行时可能显示提示。